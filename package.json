{
  "name": "retro-cssa",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node build-script.js",
    "start": "npm run build && python -m http.server 3000",
    "dev": "npm run build"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1"
  }
}
